@model IEnumerable<ExerciseGO.Models.TargetArea>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TargetAreaName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TargetAreaName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TargetAreaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TargetAreaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TargetAreaID })
        </td>
    </tr>
}

</table>
