@model IEnumerable<ExerciseGO.Models.Emoji>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmojiName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <img src="@Html.DisplayFor(modelItem => item.EmojiName)"
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmojiID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmojiID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmojiID })
        </td>
    </tr>
}

</table>
